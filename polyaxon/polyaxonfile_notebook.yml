---
version: 1

kind: notebook

environment:
  node_selector:
    aws/type: p3.2xlarge
  resources:
    gpu:
      requests: 1
      limits: 1

build:
  image: taesungp/pytorch-cyclegan-and-pix2pix
  build_steps:
    - pip3 install jupyter
